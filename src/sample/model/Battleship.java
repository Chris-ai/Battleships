package sample.model;

public class Battleship extends ShipModel {

    public Battleship(int X, int Y, int length, int demage, int orientation) {
        super(X, Y, length, demage, orientation);
    }
}